[package]
name = "gt6"
version = "0.1.0"
edition = "2021"
description = "A GTA-inspired top-down open-world game, 100% Rust compiled to WebAssembly"
license = "MIT"

[lib]
crate-type = ["cdylib", "rlib"]
path = "src/lib.rs"

[profile.release]
opt-level = "s"
lto = true

[dependencies]
wasm-bindgen = "0.2"
js-sys = "0.3"

[target.'cfg(target_arch = "wasm32")'.dependencies]
web-sys = { version = "0.3", features = [
    "Window",
    "Document",
    "Element",
    "HtmlCanvasElement",
    "CanvasRenderingContext2d",
    "KeyboardEvent",
    "Event",
    "AudioContext",
    "AudioContextOptions",
    "OscillatorNode",
    "OscillatorType",
    "GainNode",
    "AudioParam",
    "AudioScheduledSourceNode",
    "AudioDestinationNode",
] }
